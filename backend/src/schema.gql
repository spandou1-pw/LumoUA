# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type FeedConnection {
  items: [PostGraphQLType!]!
  nextCursor: String
}

enum FeedType {
  FOLLOWING
  GLOBAL
}

type PostGraphQLType {
  authorId: ID!
  body: String
  commentCount: Int!
  createdAt: DateTime!
  id: ID!
  likeCount: Int!
  viewerHasBookmarked: Boolean!
  viewerHasLiked: Boolean!
  visibility: String!
}

type Query {
  """
  doc 22: feed(type, cursor) — resolves post + author + media + viewer engagement state in one round trip
  """
  feed(cursor: String, limit: Float = 20, type: FeedType! = FOLLOWING): FeedConnection!
}